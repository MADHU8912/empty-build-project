name: Empty Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  empty-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Show Message
        run: echo "Empty GitHub Actions build successful"

      - name: List Files
        run: ls
